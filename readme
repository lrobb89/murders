we analyze us gun murder data collected by the FBI.

download-data.R - downloads the csv file to data directory

wrantle-data.r - creates a derived dataset and saves as R object in rda directory

analysis.R - generates a plot and saves it to the figs directory

